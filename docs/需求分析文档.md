# 作业监控 Dashboard 需求分析文档

| 项 | 内容 |
|---|---|
| 文档版本 | v0.2 |
| 编写日期 | 2026-07-16（v0.1）/ 2026-07-20（v0.2） |
| 文档状态 | 待评审 |
| 关联文档 | 设计文档.md |

> **v0.2 变更摘要**：根据初版上线后的生产反馈与新增功能迭代，补充鉴权（第 4.5 节）、日志分页与段落定位（4.4.2）、日志文件输出与滚动（第 5 节）、双 Y 轴趋势（4.1.2）、作业状态有效判定规则（4.2.5、4.6）、上游 `params` 字段约定（6.1）等内容；原第 9 节遗留问题 Q6 已实现，更新状态。详见文末"变更记录"。

---

## 1. 文档概述

### 1.1 目的
本文档对「作业监控 Dashboard」的需求进行整理与分析，明确功能边界、用户场景、接口现状与差距、非功能性要求，作为后续设计与开发的依据。

### 1.2 范围
覆盖以下功能域：
- 作业总体运行状态监控（状态统计、趋势比对、用户 Top10）。
- 实时作业详情表（分页、过滤、自动/手动刷新、手风琴展开）。
- 作业控制（终止、重试；暂停/恢复预留）。
- 日志查看（list 日志、LOG 日志；AI 日志分析预留）。
- 用户登录注册与作业控制鉴权（v0.2 新增，见 4.5）。

不在本期范围：作业提交、调度策略配置、用户与权限管理后台、计费统计。

### 1.3 术语

| 术语 | 含义 |
|---|---|
| 作业监控服务 | 已上线、提供 RESTful 接口 `http://130.1.100.152:18080/job/call` 的服务 |
| BFF | 新增的 Go 后端服务，作为前端与作业监控服务/文件系统/AI 服务之间的聚合层 |
| JobState | 作业状态枚举，共 13 个值 |
| 工区 survey / 项目 project | 作业所属业务对象，记录于作业 attributes |
| 采样 | BFF 周期性调用作业监控服务接口并将结果持久化的动作 |

---

## 2. 项目背景

已有「作业监控服务」通过统一入口 `POST http://130.1.100.152:18080/job/call`、以 `method` 字段区分操作，提供部分能力：当前聚合统计（`GetCurrentJSFInfo`）、作业列表查询（`GetJob`）、终止作业（`Delete`）、重试作业（`Rerunmulti`）。

该服务**不提供**：历史时序数据、按用户维度的聚合统计、作业暂停/恢复、日志文件读取。本期 Dashboard 需在前端之上新增 Go BFF 层补齐上述能力。

### 2.1 作业状态枚举

```txt
enum JobState {
  jsFrozen,      // 0  冻结
  jsQueue,       // 1  排队
  jsScheduled,   // 2  已调度
  jsReady,       // 3  就绪
  jsReleased,    // 4  已释放
  jsActive,      // 5  运行中
  jsSuspending,  // 6  挂起中
  jsSuspended,   // 7  已挂起
  jsResuming,    // 8  恢复中
  jsCanceling,   // 9  取消中
  jsFinished,    // 10 已完成
  jsFailed,      // 11 失败
  jsCanceled,    // 12 已取消
};
```

### 2.2 状态映射约定（已确认）

- `jobStatus` 字段（字符串形式数字）按枚举下标映射到 JobState，例如 `"10"` → `jsFinished`。
- `GetCurrentJSFInfo` 聚合字段与枚举映射：

  | 接口字段 | 对应 JobState | 前端展示分组 |
  |---|---|---|
  | `active` | jsActive(5) | 运行中 |
  | `queue` | jsQueue(1) | 排队中（聚合展示时含 jsFrozen/jsScheduled/jsReady/jsReleased） |
  | `finish` | jsFinished(10) | 已完成 |
  | `failed` | jsFailed(11) | 失败 |
  | `canceled` | jsCanceled(12) | 已取消 |
  | `othercount` | 其余状态（2/3/4/6/7/8/9） | 其他 |

  > 注：`queue` 字段在接口侧仅返回 jsQueue 计数，前端"排队中"展示数字以接口 `queue` 为准；如需展示"等待类"合计，将在 BFF 侧基于 GetJob 采样补齐（见 4.1.1）。

### 2.3 作业状态有效判定规则（v0.2 新增，生产确认）

生产环境中存在 `jobStatus=jsFinished(10)` 但 `exitCode!=0` 的作业，表示作业进程已结束但执行失败。仅凭 `jobStatus` 无法区分"真正完成"与"执行失败"，故定义**有效状态（EffectiveStatus）**判定规则：

- `jobStatus=jsFinished(10) 且 exitCode==0` → **真正完成**，计入"已完成"。
- `jobStatus=jsFinished(10) 且 exitCode!=0` → **执行失败**，统一视为 `jsFailed(11)`，计入"失败"。
- 其他状态原样保留。

该规则应用于：
1. 作业详情表的状态展示、状态标签、过滤匹配（见 4.2.5）。
2. Dashboard 大盘统计的"已完成"/"失败"计数（见 4.6）。
3. 作业趋势曲线的 finish/failed 数据点（见 4.6）。

> 上游 `GetCurrentJSFInfo` 聚合不感知 `exitCode`，仍按 `jobStatus` 原始值分组；BFF 端在拿到 snapshot 后，用全量作业缓存（`cache.JobCache`）统计 `jsFinished+exitCode!=0` 的数量，对 snapshot 做 `Finish -= bad / Failed += bad` 修正，再落库与返回前端。

---

## 3. 用户角色与场景

| 角色 | 场景 |
|---|---|
| 运维/管理员 | 查看整体作业运行大盘，发现异常状态（失败激增、排队堆积），定位问题作业并执行终止/重试，查看作业日志与 AI 诊断 |
| 项目负责人 | 关注本项目的作业进度与状态，按项目/工区过滤查看，必要时重试失败作业 |
| 普通用户 | 查看本人提交作业的执行情况、进度、日志 |

> v0.2 起，所有访问 Dashboard 的用户需登录（注册用户名 = 作业 `userName` 字段，用于作业所有权控制，见 4.5）。未登录访问受控接口返回 401。

---

## 4. 功能需求

### 4.1 作业监控总体信息（Dashboard 大盘）

#### 4.1.1 作业状态统计与趋势比对
- **功能**：实时展示各状态作业数量（运行中、排队中、已完成、失败、已取消、其他），以及与前一个时刻比对的上升/下降比例。
- **比对时间间隔**：**1 分钟**（已确认）。
- **数据来源**：BFF 调用 `GetCurrentJSFInfo`（实时），比对值取自 BFF 1 分钟前的采样快照。
- **展示要素**：状态名、当前数量、较 1 分钟前变化数量与百分比（↑/↓ 箭头与颜色）、更新时间。

#### 4.1.2 作业趋势
- **功能**：展示已完成、运行中、排队中、失败、已取消 5 类作业数量随时间的变化趋势。
- **时间范围**：24 小时、7 天、30 天（可切换）。
- **数据来源**：BFF 采样持久化的历史快照（见第 7 节），finish/failed 已按 4.6 规则基于 exitCode 修正。
- **展示要素**：折线图，横轴时间，纵轴作业数量；5 条曲线 + 图例可点击切换显隐。
- **双 Y 轴**（v0.2 新增，生产反馈）：生产环境"已完成"作业数量常达万级，而"运行中/排队中/失败/已取消"仅几十个，单 Y 轴会使小数值曲线被压成直线。故采用：
  - 左 Y 轴：已完成（大数值刻度）。
  - 右 Y 轴：运行中 / 排队中 / 失败 / 已取消（小数值刻度，`minInterval: 1` 保证整数）。
  - 用户可通过图例点击隐藏"已完成"曲线，单独观察小数值变化。
- **精度**：24h → 分钟级（1 分钟一个点）；7d → 小时级；30d → 天级。

#### 4.1.3 作业量用户 Top10
- **功能**：展示发送作业数量 Top10 的用户，显示用户名、数量、百分比占比。
- **统计口径**：以"当前调度中的作业"为准（基于 GetJob 全量采样的 userName 聚合）。统计窗口需确认（默认取最近 24h 提交的作业）；Top10 以外合并为"其他"。
- **数据来源**：BFF 周期性调用 `GetJob` 全量采样（分页拉取），按 userName 聚合持久化。
- **展示要素**：横向柱状图或排行表，用户名、作业数、百分比。

### 4.2 实时作业详情表

#### 4.2.1 列表展示
- **字段**：作业名称、状态、用户、进度、工区、项目、运行时间、提交时间、操作。
- **分页**：BFF 在内存全量作业快照上做本地过滤后分页（详见 4.2.2 与设计文档）。GetJob 上游分页仅用于 BFF 拉取全量快照，不直接暴露给前端。
- **状态展示**：状态枚举映射为中文标签与颜色标签（运行中-绿、排队中-蓝、已完成-灰、失败-红、已取消-橙、其他-默认）。
- **进度**：百分比进度条。
- **时间**：提交时间、运行时间（execTime）按可读格式展示。

#### 4.2.2 过滤
- **支持字段**（单一或组合、均支持多选）：
  - 作业状态 `jobStatus`（多选，枚举值）
  - 项目 `project`（多选）
  - 工区 `survey`（多选）
  - 用户 `userName`（多选）
  - 作业名称 `jobDesc`（单值文本，对作业名称做包含匹配，如名称包含 "input"）
- **多选下拉候选来源**：已有的作业监控服务**未提供**按项目/工区/用户/数据库枚举的 RESTful 接口，因此候选项由 BFF 从查询到的作业全量快照中提取 distinct 值，通过 `GET /api/v1/jobs/filters` 接口提供给前端。
- **下拉交互**：项目/工区/用户多选下拉支持 `filterable`（输入过滤）+ `collapse-tags`（折叠标签）+ `collapse-tags-tooltip`（折叠后 hover 查看全部）。
- **过滤实现**：BFF 在内存中的全量作业快照上做本地多值过滤（状态/用户/项目/工区/数据库为命中集合任一即匹配；`jobDesc` 为大小写不敏感的包含匹配），按提交时间倒序后分页返回。上游 GetJob 的 `projList`/`descFilter` 等过滤参数本期不依赖，统一在 BFF 侧本地过滤，避免上游参数语义不一致。
- **`projList` 字段格式**（已确认）：GetJob 请求/响应中的 `projList` 为对象数组，每个元素含 `database`/`project`/`survey`/`line` 四个字段，例如：
  ```json
  "projList": [{"database": "ndp_check", "project": "project1", "survey": "survey1", "line": "line1"}]
  ```
  BFF 从作业的该结构中提取 `database`/`project`/`survey` 用于展示与过滤候选。
- **`descFilter` 字段语义**（已确认）：对作业名称 `jobDesc` 做**包含**匹配（如 `descFilter="input"` 命中所有名称含 "input" 的作业）。BFF 本地过滤采用相同语义（大小写不敏感的 contains）。

#### 4.2.3 刷新
- **自动刷新间隔**：30 分钟、10 分钟、5 分钟、1 分钟、10 秒（可切换，含"关闭"选项）。
- **手动刷新**：提供刷新按钮，立即触发一次。
- **实现**：前端定时器轮询 BFF；刷新时保留当前过滤与分页状态。

#### 4.2.4 手风琴展开
- 点击作业行展开，展示基本信息摘要：作业编号、当前状态、所属用户、项目、提交时间、作业名称、执行进度、工区、运行耗时、退出码、摘要（总结作业运行情况）。
- **退出码展示**（v0.2 新增）：`exitCode=0` 灰色文本"0"；`exitCode!=0` 红色 tag 突出显示，便于快速识别执行失败的作业。
- **摘要**：基于作业字段拼装（如"作业 {jobDesc} 由 {userName} 于 {提交时间} 提交，已执行 {execTime}，进度 {jobProcess}%，状态 {状态}"）。

#### 4.2.5 状态展示与 exitCode 判定（v0.2 新增）
- 作业状态字段 `jobStatus` 与 `jobStatusLabel` 的取值按 2.3 节"有效状态"规则计算：
  - `jobStatus=jsFinished(10) 且 exitCode==0` → `jobStatusLabel="已完成"`，颜色灰（info）。
  - `jobStatus=jsFinished(10) 且 exitCode!=0` → 视为 `jsFailed(11)`，`jobStatusLabel="失败"`，颜色红（danger）。
- **过滤匹配**同样使用有效状态：前端"失败"过滤（`jobStatus=11`）能同时命中原本 `jsFailed` 与 `jsFinished+exitCode!=0` 的作业；前端"已完成"过滤（`jobStatus=10`）只返回真正 `exitCode==0` 的作业。
- **重试按钮**：有效状态为 `jsFailed` 或 `jsCanceled` 时可点；原本 `jsFinished+exitCode!=0` 的作业也可重试。

### 4.3 作业控制

| 操作 | 接口 | 状态 |
|---|---|---|
| 终止 | `Delete` | 已有 |
| 重试（失败作业重试） | `Rerunmulti` | 已有 |
| 暂停 / 恢复 | 无 | **预留 UI，接口待定** |

- **操作入口**：详情表"操作"列与展开面板内。
- **交互**：操作前二次确认（弹窗）；批量操作支持（勾选多作业后批量终止/重试）。
- **暂停/恢复**：本期预留按钮，点击提示"接口待提供"，不调用后端；待服务端提供接口后对接。
- **反馈**：操作成功/失败 Toast 提示，并自动刷新当前列表。

### 4.4 日志查看

#### 4.4.1 日志定位
- **list 日志**：`{项目主目录}/data/{project}/{survey}/list`
- **LOG 日志**：`{项目主目录}/data/{project}/{survey}/LOG`
- **项目主目录解析**：读取环境变量 `$NGP` 指向的 `configs/ndp/projects.conf`（XML 配置文件），按作业的 `project` 查找其 `maindir`，得到项目主目录。
- **`projects.conf` 结构**（已确认，文件已上传至 `docs/projects.conf`）：根元素 `<Projects>`，下含多个 `<project>` 子元素，每个 `<project>` 包含 `<name>`、`<dbname>`、`<maindir>` 等子元素。示例：
  ```xml
  <Projects>
    <project>
      <name>qqqq</name>
      <dbname>ndp</dbname>
      <maindir>D:\iEcoData\GeoEastData\DATA</maindir>
      ...
    </project>
  </Projects>
  ```
  BFF 按 `<name>` 匹配作业的 `project`，取对应 `<maindir>` 作为项目主目录。
- **路径安全**：拼接后的工区目录需做 `filepath.Clean` 规范化并校验是否仍以 `maindir` 为前缀，防止 `../` 路径逃逸。

#### 4.4.2 日志展示
- 分别查看 list 日志与 LOG 日志。
- 收集并分类展示 Info / Warn / Error 信息（按日志级别解析）。
- 支持级别过滤、关键字搜索、滚动加载（日志可能较大）。

#### 4.4.3 AI 日志分析
- **功能**：日志信息汇总、模块错误分布、诊断建议。
- **状态**：**预留接口待定**（AI 服务与模型后续确认）。BFF 设计可插拔适配层，本期可先以"规则诊断"占位（基于关键字/错误码的模块错误统计与常见建议库），待 AI 服务确认后替换。

#### 4.4.4 日志分页与段落快速定位（v0.2 新增）
- **分页**：日志可能很大（数万行），BFF 不再一次返回全部内容；接口支持 `page` 与 `pageSize` 参数，BFF 按页返回 `lines`、`total`（未知时返回 -1）、`truncated` 标记。
- **list 段落导航**：list 日志通常按业务段落分隔（如 `===== Section: xxx =====`）。BFF 在首次读取时扫描识别段落（`name` + `lineNo`），通过响应的 `sections` 字段返回给前端；前端在段落下拉中选中后，跳转到对应 `page`，便于快速定位而非逐页翻找。
- **多文件支持**：LOG 日志按日期/序号可能切分为多个文件（`LOG`、`LOG.1`、`LOG.2`…），BFF 返回 `files` 列表，前端可切换文件查看。
- **过滤标记**：当请求带级别或关键字过滤时，响应 `filtered=true`，前端提示"当前为过滤结果"。

### 4.5 用户登录注册与作业控制鉴权（v0.2 新增）

#### 4.5.1 注册与登录
- **注册**：用户名 + 密码，密码使用 bcrypt 哈希存储；用户名作为后续作业所有权的依据（与作业 `userName` 字段对齐）。
- **登录**：校验通过后下发 Token（自定义格式 `payload.signature`，payload 为 base64 编码的 `{username, exp}` JSON，签名用 HMAC-SHA256 + 服务端密钥）。
- **Token 时效**：默认 24 小时，可配（`auth.tokenTTLHour`）。
- **Token 传递**：前端通过 `Authorization: Bearer <token>` 头部携带。

#### 4.5.2 鉴权中间件
- 受控接口（作业控制、日志查看、AI 分析等）走 `AuthMiddleware`：校验 Token 签名与时效，失败返回 401。
- 大盘统计等只读公开接口可豁免鉴权（视部署策略而定，默认需登录）。

#### 4.5.3 作业所有权控制
- 作业的 `userName` 字段即作业所有者。
- 终止、重试等控制操作：仅作业所有者可对自己名下的作业执行；非所有者操作返回 403。
- 列表查看：默认显示全量作业；前端提供"仅看我的作业"开关，开启后只列出当前登录用户名下的作业。

### 4.6 统计与趋势的 exitCode 修正（v0.2 新增）

Dashboard 大盘统计与趋势曲线的"已完成"/"失败"计数必须遵循 2.3 节的有效状态规则：

- 上游 `GetCurrentJSFInfo` 聚合不感知 `exitCode`，`jsFinished+exitCode!=0` 的作业会被算入 `finish`。
- BFF 在以下两条路径上做修正：
  1. **采样落库**（`jsf_sampler`）：每分钟从上游拿到 snapshot 后，用 `cache.JobCache` 全量作业统计 `jsFinished+exitCode!=0` 的数量 `bad`，对 snapshot 做 `Finish -= bad / Failed += bad`，再写 `job_stats_snapshot` 表。趋势接口读 DB 自动反映修正后的值。
  2. **实时统计接口**（`/dashboard/stats?fresh=1`）：直读上游的两条路径（fresh 失败回退 DB、fresh 成功、DB 无快照回退上游）同样调用 `correctByExitCode` 修正。
- 修正依赖 `cache.JobCache` 非空；首启时 job 采样先于 jsf 采样执行，保证 jsf 第一次能用到缓存。
- 若 `cache.JobCache` 为空（如上游 GetJob 不可用），跳过修正，保留上游原始值。

---

## 5. 非功能需求

| 维度 | 要求 |
|---|---|
| 性能 | 大盘首屏 ≤ 2s；详情表分页查询 ≤ 1s；30 天趋势查询 ≤ 2s |
| 采样频率 | BFF 对 GetCurrentJSFInfo 采样间隔 1 分钟；GetJob 全量采样间隔可配（默认 1 分钟），避免对作业监控服务造成压力 |
| 可用性 | BFF 单实例部署，依赖作业监控服务可用；作业监控服务不可用时前端展示降级提示与最近一次缓存数据；`/dashboard/stats` 接口在上游失败时返回 200 + `degraded:true`，避免前端 500 阻塞 |
| 浏览器兼容 | Chrome/Edge 最新版（基于 Vue3 + 现代构建） |
| 可维护性 | 前后端分离，BFF 配置化（采样间隔、接口地址、日志根路径、AI 适配） |
| 安全 | v0.2 起所有受控接口需登录鉴权（见 4.5）；BFF 内网部署；日志读取需做路径越权校验（防止 `../` 逃逸） |
| 数据保留 | 采样快照保留 30 天（满足 30 天趋势），超期清理 |
| BFF 日志输出（v0.2 新增） | BFF 进程日志同时输出到 stdout 与文件（`log.file` 配置项）；按大小滚动切分（`maxSizeMB`，默认 100MB），保留最近 `maxBackups` 份（默认 7）与 `maxAgeDays` 天（默认 30）；滚动时将当前文件重命名为 `<file>.<timestamp>.log` 并创建新文件 |

---

## 6. 接口现状与差距分析

### 6.1 已有接口（作业监控服务提供）
统一入口 `POST http://130.1.100.152:18080/job/call`：

| method | 用途 | 备注 |
|---|---|---|
| `GetCurrentJSFInfo` | 当前聚合统计 | 仅当前快照，无历史 |
| `GetJob` | 作业列表查询 | 支持分页与过滤 |
| `Delete` | 终止作业 | 入参 name 列表 |
| `Rerunmulti` | 重试作业 | 入参 name 列表 |

> **请求体字段约定**（v0.2 确认，生产踩坑）：所有 method 的请求体统一使用 **`params`（复数）** 字段承载业务参数，**不存在 `param`（单数）字段**。早期 BFF 代码误用 `param` 单数形式，导致上游返回 `errorCode=1` 与 500 错误。`GetCurrentJSFInfo` 的 `params` 为 `{"type": 2}`；`GetJob` 的 `params` 含 `type`/`page`/`pageSize`/`filterType`/`projList`/`descFilter` 等字段，其中 `filterType` 默认 `0`（已确认）。

> **响应字段约定**：上游响应统一为 `{ "errorCode": 0, "count": N, "result": <payload> }`，`errorCode != 0` 表示业务错误，BFF 应将 `result` 截断后记入错误日志便于排查。

### 6.2 差距（由 BFF 补齐）

| 需求 | 差距 | BFF 方案 |
|---|---|---|
| 趋势比对（1 分钟前） | 无历史 | 定时采样 GetCurrentJSFInfo 持久化 |
| 24h/7d/30d 趋势 | 无时序数据 | 复用上述采样快照，按粒度聚合 |
| 用户 Top10 | 无按用户聚合 | 定时全量采样 GetJob，按 userName 聚合持久化 |
| 多选过滤（项目/工区/用户/数据库） | 上游无枚举接口、GetJob 过滤参数语义不确定 | BFF 维护全量作业内存快照，本地多值过滤 + distinct 候选值接口 |
| 暂停/恢复 | 无接口 | 前端预留，待服务端 |
| 日志读取 | 无文件接口 | BFF 读文件系统（projects.conf 解析路径） |
| AI 日志分析 | 无 AI 能力 | BFF 适配层，规则诊断占位，AI 待定 |

---

## 7. 数据需求（采样持久化）

BFF 需持久化以下数据（具体表结构见设计文档）：

1. **状态快照时序**：每分钟一条，记录 active/queue/finish/failed/canceled/othercount 及时间戳，用于趋势比对与 24h/7d/30d 趋势。
2. **作业全量快照（内存）**：每分钟一次全量采样（GetJob 分页拉取），写入 BFF 内存缓存（`cache.JobCache`），用于：
   - 用户 Top10 聚合（按 userName 聚合后存"用户作业量时序"，避免全量明细落库膨胀）；
   - 作业详情表的**多选过滤**与**候选值提取**（项目/工区/用户/数据库 distinct 值，供前端下拉）。
   全量明细仅驻内存不落库；列表/过滤/候选值均在内存快照上本地计算。
3. **保留策略**：30 天滚动清理。

> 说明：Top10 统计窗口与"当前调度中"范围需在评审时最终确认（见第 9 节）。

---

## 8. 假设与依赖

- 作业监控服务 `http://130.1.100.152:18080/job/call` 在内网稳定可用。
- BFF 部署机器能够：
  - 访问作业监控服务；
  - 访问作业日志所在文件系统（即 `$NGP` 配置指向的项目主目录可读）。
- `projects.conf` XML 配置文件可被 BFF 读取，文件已上传至 `docs/projects.conf`，结构为 `<Projects><project><name>/<dbname>/<maindir>/...</project></Projects>`（已确认）。
- 状态映射按枚举下标（已确认）。
- 比对时间间隔 1 分钟（已确认）。
- AI 服务后续提供，本期预留。

---

## 9. 待确认 / 遗留问题

| 编号 | 问题 | 当前处理 |
|---|---|---|
| Q1 | `GetJob` 的 `projList`、`descFilter` 字段含义与格式 | **已确认**（见 4.2.2）：`projList` 为含 database/project/survey/line 的对象数组；`descFilter` 对 `jobDesc` 做包含匹配。BFF 本地过滤采用同语义，不依赖上游过滤参数。 |
| Q2 | Top10 统计窗口（最近 24h 提交？当前调度中？） | 默认"当前调度中"（基于全量采样），评审确认 |
| Q3 | `projects.conf` XML 结构 | **已确认**（见 4.4.1）：文件已上传至 `docs/projects.conf`，结构为 `<Projects><project><name>/<dbname>/<maindir>/...</project></Projects>`。 |
| Q4 | AI 服务模型/endpoint/key | 本期预留适配层，规则诊断占位 |
| Q5 | 暂停/恢复接口契约 | 前端预留 UI，待服务端提供 |
| Q6 | 是否需要登录鉴权与数据权限 | **v0.2 已实现**：用户登录注册 + 作业所有权控制（见 4.5），受控接口走 AuthMiddleware |
| Q7 | BFF 部署位置与端口 | 已定：BFF 监听 18088，前端由 BFF 同源静态托管（见设计文档） |
| Q8 | 持久化存储选型（SQLite/MySQL） | 已定：SQLite（纯 Go 驱动 `modernc.org/sqlite`，免 CGO） |
| Q9 | 多选过滤候选值来源 | **已确认**：上游无枚举接口，BFF 从全量作业快照提取 distinct project/survey/user/database，经 `GET /api/v1/jobs/filters` 提供 |
| Q10 | 上游请求体字段是 `param` 还是 `params`？ | **v0.2 已确认**：统一使用 `params`（复数），见 6.1 |
| Q11 | `jsFinished+exitCode!=0` 是否应视为失败？ | **v0.2 已确认**：是，按 2.3 节有效状态规则处理，见 4.2.5 与 4.6 |
| Q12 | 趋势图单 Y 轴看不清小数值变化 | **v0.2 已解决**：改双 Y 轴，见 4.1.2 |

---

## 10. 评审清单

评审时请重点关注：
1. 第 2.2 节状态映射是否符合服务端实际。
2. 第 2.3 节作业状态有效判定规则（`jsFinished+exitCode!=0` 视为失败）是否认可。
3. 第 4.1.2 节趋势图双 Y 轴方案是否认可。
4. 第 4.1.3 节 Top10 统计窗口。
5. 第 4.2.2 节多选过滤实现（BFF 内存快照本地过滤 + 候选值接口）是否认可。
6. 第 4.4.1 节 `projects.conf` 解析与跨平台路径（maindir 为 Windows 盘符）。
7. 第 4.5 节鉴权与作业所有权控制是否认可。
8. 第 6.1 节上游 `params` 字段约定是否符合服务端实际。
9. 第 9 节遗留问题 Q1~Q12 的处置。

---

## 11. 变更记录

| 版本 | 日期 | 变更内容 | 对应提交 |
|---|---|---|---|
| v0.1 | 2026-07-16 | 初版：作业监控大盘、详情表、控制、日志、AI 预留 | `b77aa45` |
| v0.2 | 2026-07-20 | 新增鉴权与作业所有权控制（4.5）；日志分页与段落快速定位（4.4.4）；BFF 日志文件输出与滚动切分（第 5 节）；趋势图扩展为 5 条曲线 + 双 Y 轴（4.1.2）；新增作业状态有效判定规则（2.3、4.2.5、4.6）；明确上游 `params` 字段约定（6.1）；上游失败时 stats 接口降级返回（第 5 节）；更新遗留问题 Q6/Q10/Q11/Q12 | `7a1adf2` `f7ceef7` `f43af56` `7b8111e` `d8f91cb` `d8f2e20` `517b2c8` `4b6eddf` |
